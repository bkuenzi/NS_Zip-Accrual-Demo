Subject: Accrual confirmation request — {{ vendor_name }} — {{ period }} {{ ref_token }}

Hello {{ contact_name }},

As part of our {{ period }} financial close, we're confirming amounts for goods
or services {{ company_name }} received from {{ vendor_name }} during the period
that we have not yet been invoiced for.

Our records show the following incurred-but-uninvoiced amount:

    Reference:        {{ source_ref }}
    Service period:   {{ period }}
    Estimated amount: {{ amount }} {{ currency }}

Could you confirm the amount above (or give us the correct one)? The quickest
way is to fill in this block in your reply:

    AMOUNT:
    CURRENCY: {{ currency }}
    DELIVERED PERCENT (services only — share of the work delivered):
    INVOICE NUMBER (if issued):
    EXPECTED INVOICE DATE (YYYY-MM-DD):

A short free-text reply works too — please keep {{ ref_token }} in the subject
line so it routes correctly.

Thank you,
{{ company_name }} Accounting
{{ mailbox_address }}
